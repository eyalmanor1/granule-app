This build is prepared for GitHub Pages WITHOUT Service Worker / PWA install.
- manifest link removed from index.html
- service worker registration removed from index.html
- service-worker.js and manifest files removed (if existed)
If you later want to re-enable PWA, restore manifest link and SW registration.
